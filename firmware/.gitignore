# PlatformIO
.pio/
.vscode/

# ESP-IDF build output + generated config
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Vendored Arduino libraries for the ESP-IDF build (see components/README.md).
# The wrapper CMakeLists.txt is tracked; the copied library sources are not.
components/Arduino_GFX/src/
components/NimBLE-Arduino/src/
